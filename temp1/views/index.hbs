<!DOCTYPE html>
<html>

<head>
      <title>Home page</title>
      <link rel="stylesheet" href="css/style.css" />
</head>

<body>
      <h2>{{>header}} </h2>
      <section class="home-section" id="home">
            <div class="container">
                  <div class="row align-items-center">
                        <div class="home-text">
                              <h4>{{maintitle}}</h4>
                              <h1>{{subtitle}}</h1>
                              <span>{{title}}</span>
                              <p>{{desc}}</p>
                              <a href="/login" class="btn btn-1">{{login}}</a>
                              <a href="/signup" class="btn btn-1">{{signup}}</a>
                        </div>
                        <div class="home-image">
                              <div class="img-box">
                                    <img src={{img}} alt="">
                              </div>
                        </div>
                  </div>
            </div>
            <div class="header-hero-shape"></div>
        <h2>{{>footer}} </h2>

      </section>





</body>

</html>